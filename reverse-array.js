//set of numbers that needs a reverse order.
const setNums = [1, 3, 5, 7, 9, 11];

// A set of numbers needs to be analyzed and the order of the set needs to be reversed afterwards.

//If a set of numbers looks like this: [1, 3, 5, 7, 9, 11], then the expected output should look like this: [ 11, 9, 7, 5, 3, 1 ]

//output
console.log(setNums.reverse());